<component-set>
    <components>
        <component>
            <role>
                org.apache.maven.lifecycle.mapping.LifecycleMapping
            </role>
            <role-hint>npm</role-hint>
            <implementation>
                org.apache.maven.lifecycle.mapping.DefaultLifecycleMapping
            </implementation>
            <configuration>
                <lifecycles>
                    <lifecycle>
                        <id>default</id>
                        <phases>
                            <initialize>${project.groupId}:${project.artifactId}:${project.version}:generate-project</initialize>
                            <generate-sources>
                                com.github.eirslett:frontend-maven-plugin:install-node-and-npm,
                                ${project.groupId}:${project.artifactId}:${project.version}:npm-install
                            </generate-sources>
                            <compile>${project.groupId}:${project.artifactId}:${project.version}:compile</compile>
                            <package>
                                ${project.groupId}:${project.artifactId}:${project.version}:package
                            </package>
                            <install>org.apache.maven.plugins:maven-install-plugin:install</install>
                            <deploy>${project.groupId}:${project.artifactId}:${project.version}:deploy,
                                org.apache.maven.plugins:maven-deploy-plugin:deploy
                            </deploy>
                        </phases>
                    </lifecycle>
                </lifecycles>
            </configuration>
        </component>
        <component>
            <role>org.apache.maven.artifact.handler.ArtifactHandler</role>
            <role-hint>npm</role-hint>
            <implementation>
                org.apache.maven.artifact.handler.DefaultArtifactHandler
            </implementation>
            <configuration>
                <type>tgz</type>
                <extension>tgz</extension>
                <language>typescript</language>
                <addedToClasspath>false</addedToClasspath>
            </configuration>
        </component>
    </components>
</component-set>